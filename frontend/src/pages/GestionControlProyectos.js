/** Pantalla de gestión (proyectos CRUD) — misma lógica que /boletas/gestion */
import ControlProyectos from './ControlProyectos';

export default function GestionControlProyectos() {
  return <ControlProyectos modoGestion />;
}
