/**
 * Reembolsos: el aprobador y el destinatario de los correos de solicitud se resuelven
 * con Empleado.obtenerAprobadorReembolsos() (registro en tabla empleados).
 *
 * Opcional en .env:
 *   REEMBOLSOS_APROBADOR_EMPLEADO_ID=<id>  → fuerza qué fila de empleados es el aprobador
 */

module.exports = {};
